let zufallzahl1 = Math.round(Math.random()*100)
let zufallszahl2 = Math.round(Math.random()*100)
let winstreak
let startwert = 50

function höher(){
    if(zufallzahl1 > startwert) {
        document.getElementById("tf")
    }
}